{% extends 'base.html.twig' %}

{% block title %}Hoursworked{% endblock %}

{% block body %}
    <h1>Hoursworked</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hoursworked.id }}</td>
            </tr>
            <tr>
                <th>Period</th>
                <td>{{ hoursworked.period }}</td>
            </tr>
            <tr>
                <th>NumberHours</th>
                <td>{{ hoursworked.numberHours }}</td>
            </tr>
            <tr>
                <th>WorkDays</th>
                <td>{{ hoursworked.workDays ? hoursworked.workDays|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_hoursworked_index') }}">back to list</a>

    <a href="{{ path('app_hoursworked_edit', {'id': hoursworked.id}) }}">edit</a>

    {{ include('hoursworked/_delete_form.html.twig') }}
{% endblock %}
