{% extends 'base.html.twig' %}

{% block title %}Positions{% endblock %}

{% block body %}
    <h1>Positions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ position.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ position.type }}</td>
            </tr>
            <tr>
                <th>HourlyRate</th>
                <td>{{ position.hourlyRate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_positions_index') }}">back to list</a>

    <a href="{{ path('app_positions_edit', {'id': position.id}) }}">edit</a>

    {{ include('positions/_delete_form.html.twig') }}
{% endblock %}
